name: logistics_app
description: "مدیریت بسته‌ها با نقشه و مسیریابی نشن"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  mobile_scanner: ^4.0.1
  geolocator: ^10.0.0
  http: ^1.1.0
  flutter_map: ^6.0.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
