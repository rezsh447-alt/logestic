name: logistics_app
description: "مدیریت بسته‌ها با نقشه نشن"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  mobile_scanner: ^4.0.1
  neshan_flutter: ^0.1.4
  geolocator: ^10.0.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true